# Service models vs deployment models, what's the difference?

Cloud deployment models (public, private, hybrid) define where the cloud service is hosted and managed. Cloud service models (SaaS, PaaS, IaaS) define what is offered.
