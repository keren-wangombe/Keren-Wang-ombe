# What happens when a user's group policies conflict?

In AWS IAM, conflicting policies are resolved by clear rules, the most prominent being that an explicit Deny always takes priority over an Allow.
