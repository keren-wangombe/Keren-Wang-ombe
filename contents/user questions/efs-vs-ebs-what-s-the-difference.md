# EFS vs EBS, what's the difference?

Both EFS and EBS are storage services in AWS. EFS (Elastic File System) is designed for use across multiple EC2 machines and Availability Zones, while EBS (Elastic Block Store) is designed as fast, reliable block storage for a single machine.
